import { FilterOpsForTypePipe } from './filter-ops-for-type.pipe';

describe('FilterOpsForTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOpsForTypePipe();
    expect(pipe).toBeTruthy();
  });
});
