import { AllIncomesPipe } from './all-incomes.pipe';

describe('AllIncomesPipe', () => {
  it('create an instance', () => {
    const pipe = new AllIncomesPipe();
    expect(pipe).toBeTruthy();
  });
});
