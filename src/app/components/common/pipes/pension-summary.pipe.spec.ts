import { PensionSummaryPipe } from './pension-summary.pipe';

describe('PensionSummaryPipe', () => {
    it('create an instance', () => {
        const pipe = new PensionSummaryPipe();
        expect(pipe).toBeTruthy();
    });
});
