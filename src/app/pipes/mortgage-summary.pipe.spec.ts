import { MortgageSummaryPipe } from './mortgage-summary.pipe';

describe('MortgageSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new MortgageSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
