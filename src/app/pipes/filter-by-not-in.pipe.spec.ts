import { FilterByNotInPipe } from './filter-by-not-in.pipe';

describe('FilterByNotInPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNotInPipe();
    expect(pipe).toBeTruthy();
  });
});
