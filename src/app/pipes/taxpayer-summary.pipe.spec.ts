import { TaxpayerSummaryPipe } from './taxpayer-summary.pipe';

describe('TaxpayerSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new TaxpayerSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
